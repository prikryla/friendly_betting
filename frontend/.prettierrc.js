module.exports = {
    useTabs: false, // Indent lines with tabs instead of spaces.
    printWidth: 128, // Line width of 80 characters
    tabWidth: 4, // Tab width of 4 spaces
    singleQuote: true, // Use single quotes
    trailingComma: "all",  // Always use trailing commas
    bracketSameLine: false, // Put the > of a multi-line HTML element at the end of the last line instead of being alone on the next line 
    arrowParens: "always" // Include parentheses around a sole arrow function parameter.
}; 